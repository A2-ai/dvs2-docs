+++
title = "Reference"
sort_by = "weight"
weight = 4
template = "section.html"
description = "Storage internals, configuration, the audit log, and the error surface."
+++

Deeper reference material that applies whether you drive dvs from R or the CLI:
how files are stored and addressed, the `dvs.toml` configuration, the
append-only audit log, and the full set of errors dvs can raise.
